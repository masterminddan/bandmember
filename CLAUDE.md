# Project instructions

## Git

- Commit and push directly to `master`. Do not create a feature branch for
  changes on this project (overrides the global "branch first on the default
  branch" default).
